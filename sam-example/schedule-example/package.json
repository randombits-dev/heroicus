{
  "name": "server-manager",
  "version": "1.0.0",
  "description": "hello world sample for NodeJS",
  "main": "server-manager.js",
  "license": "MIT",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit",
    "build server manager": "sam build",
    "run server manager": "sam local invoke"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.332.0",
    "esbuild": "^0.14.14",
    "viem": "^0.3.50"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "@types/jest": "^29.2.0",
    "@types/node": "^18.11.4",
    "jest": "^29.2.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
